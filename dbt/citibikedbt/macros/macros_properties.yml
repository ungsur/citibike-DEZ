version: 2

macros:
  - name: get_gender_type_description
    description: >
      This macro receives a gender and returns the corresponding description.
    arguments:
      - name: gender_type
        type: int
        description: > 
          gender_type value.
          Must be one of the accepted values, otherwise the macro will return null